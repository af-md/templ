package testcontext

type contextKey string

var contextKeyName contextKey = "name"

templ render() {
	<ul>
		<li>{ templ.Context().Value(contextKeyName).(string) }</li>
		if templ.Context().Value(contextKeyName).(string) == "test" {
			<li>the if passed</li>
		}
		if templ.Context().Value(contextKeyName).(string) != "test" {
			<li>the else if failed</li>
		} else if templ.Context().Value(contextKeyName).(string) == "test" {
			<li>the else if passed</li>
		}
	</ul>
}
